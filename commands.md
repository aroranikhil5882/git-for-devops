# Git Commands History

```bash
git init
git add nibba.txt
cd 
ls -a
cd git-for-devops
ls
ls -a
cd nibba.txt
git add nibba.txt
git status
touch nibba.txt
git add nibba.txt
git status
git commit -m "here you go git status"
git commit -m "here you go git status"
git restore nibba.txt
git status
ls
git log
vim nibba.txt
git add nibba.txt
git commit -m "Adding changes"
git log
git status
git restore nibba.txt
git status
git checkout
git checkout master
git checkout -b dev
touch nibbit.txt
ls
git checkout master
ls
git branch
git checkout dev
touch nikhil.txt
git add nikhil.txt
ls
git checkout master
ls
git checkout master
git checkout dev
ls
git checkout dev
ls
git checkout master
git checkout dev
git status
git log
history
```
